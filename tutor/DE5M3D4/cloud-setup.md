## Cloud Setup

- Assign one platform (AWS, Azure, Google Cloud) per group - if more than three groups, double up so two groups defend the same platform
- Remind them: best guess from vendor docs is fine, this is not a deep-dive
- Frame it against HomeSphere's pipeline from this week, not a generic comparison
